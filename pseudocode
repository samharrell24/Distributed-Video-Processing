# video -> seperate into frames
# individual images -> grayscale to reduce to one channel
# ffmpeg -i bookcopy.jpg -vf format=gray boxgrayscale.jpg

# https://github.com/nothings/stb/blob/master/stb_image.h
# send image through iterator
# apply sobel filter on image

# convert jpg -> bmp

